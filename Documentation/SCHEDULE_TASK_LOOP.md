======================================================================
                         БЛОК 1. CURRENT TASK
======================================================================

Пока нет активной задачи.
======================================================================
                  БЛОК 2. PREVIEW / SHORT COMMAND
======================================================================

Пока нет короткой команды.
======================================================================
                         SCHEDULE TASK LOOP
======================================================================

Документ для последующих итераций по обновлению календарных графиков занятий в DOCX-программах.

Основной рабочий DOCX-файл на текущем этапе:

`Программа/72_Ознакомительная_Бартов_2026_Зима.docx`

Рабочий файл правил и задач:

`Documentation/SCHEDULE_TASK_LOOP.md`

======================================================================
                         SYSTEM INSTRUCTIONS
======================================================================

Агент работает как исполнитель.

Правила:

1. Читать текущую задачу только из `БЛОК 1. CURRENT TASK`.
2. Выполнять только текущую задачу.
3. Использовать `БЛОК 2. PREVIEW / SHORT COMMAND` только как короткую навигационную команду.
4. Если `БЛОК 2` противоречит `БЛОК 1` или `SYSTEM INSTRUCTIONS`, приоритет имеют `SYSTEM INSTRUCTIONS` и `БЛОК 1`.
5. Противоречия между блоками записывать в `QUESTIONS`.
6. Не менять архитектуру документа без отдельного указания.
7. Не переписывать DOCX с нуля.
8. Не менять темы занятий без отдельного указания.
9. Не менять количество часов без отдельного указания.
10. Не менять количество занятий без отдельного указания.
11. Не менять второй график, если задача относится только к первому.
12. Не менять файлы вне явно указанных в `БЛОК 1. CURRENT TASK`.
13. Сохранять форматирование DOCX.
14. Если задача аналитическая — ничего не изменять.
15. Если в задаче не хватает данных — записать вопросы в `QUESTIONS`.
16. После выполнения обновить раздел `AGENT REPORT`.
17. Не удалять основные разделы этого файла.
18. После успешного выполнения задачи перенести краткое описание выполненной задачи в `PREVIOUS TASK`.
19. После успешного выполнения задачи очистить `БЛОК 1. CURRENT TASK` и оставить там текст: `Пока нет активной задачи.`
20. После успешного выполнения задачи очистить `БЛОК 2. PREVIEW / SHORT COMMAND` и оставить там текст: `Пока нет короткой команды.`
21. Игнорировать временные служебные файлы Microsoft Word вида `~$*.docx` и `~$_*.docx`. Такие файлы могут появляться, когда DOCX открыт у пользователя. Не анализировать их, не редактировать, не удалять вручную и не учитывать как рабочие документы.
22. Разовые большие задачи нельзя вставлять выше `БЛОК 1. CURRENT TASK`.
23. Если агент работает локально в папке проекта, не скачивать файлы из GitHub и не переключаться на указанный коммит без отдельного указания пользователя. Указанный коммит использовать только как ориентир / контрольную точку. При локальной работе считать актуальными файлы, которые уже лежат в рабочей папке проекта.
24. Не удалять раздел `TASK HISTORY` и не удалять старые записи из `TASK HISTORY` без отдельного указания пользователя. После важных задач добавлять краткую запись в `TASK HISTORY`.

======================================================================
                         LOCAL WORK RULES
======================================================================

Если агент работает локально в папке проекта, то актуальными считаются локальные файлы в рабочей папке.

В этом режиме:

1. Не нужно скачивать файлы из GitHub.
2. Не нужно переключаться на указанный коммит.
3. Не нужно заменять локальные файлы версиями из GitHub.
4. Указанный коммит использовать только как ориентир / контрольную точку.
5. Работать нужно с текущими локальными файлами.
6. Если пользователь явно говорит, что локальный файл уже заменён на новую актуальную версию, использовать именно локальный файл.
7. Если есть сомнение, не перезаписывать локальные файлы из GitHub, а записать вопрос в QUESTIONS.

Для текущего проекта основные локальные файлы:

- Программа/72_Ознакомительная_Бартов_2026_Зима.docx
- Documentation/SCHEDULE_TASK_LOOP.md

======================================================================
                      DOCUMENT STRUCTURE NOTES
======================================================================

В DOCX-файле есть Приложение 2.

В Приложении 2 находятся два календарных графика занятий.

График 1:

- исходно был построен по дням недели понедельник / среда;
- содержит 32 занятия;
- даты связаны с темами занятий.

График 2:

- отдельный график для другой группы / потока;
- его нельзя автоматически менять вместе с графиком 1;
- для изменения нужны отдельные вводные: дата начала и дни недели.

В папке `Программа` могут временно появляться служебные lock-файлы Microsoft Word, например:

`Программа/~$_Ознакомительная_Бартов_2026_Зима.docx`

Это не рабочие документы. Они создаются при открытии DOCX на стороне пользователя и обычно удаляются автоматически после закрытия Word.

======================================================================
                      DATE CALCULATION RULES
======================================================================

При расчёте нового графика использовать производственный календарь РФ на соответствующий год.

Общие правила:

1. Занятия идут только по указанным дням недели.
2. Количество занятий сохраняется.
3. Если дата занятия попадает на официальный нерабочий праздничный день РФ — эту дату пропустить.
4. После пропуска продолжить график по следующему подходящему учебному дню.
5. Если рабочий день перенесён на субботу, субботу не добавлять автоматически, если группа не занимается по субботам.
6. Не додумывать новые дни занятий без указания пользователя.
7. Если есть сомнение по праздничному дню или переносу — записать это в `QUESTIONS`, а не менять график наугад.

======================================================================
                         TASK FORMAT RULES
======================================================================

Для последующих итераций пользователь может давать задачу в двух сообщениях.

Сообщение 1 — большой блок задачи.

Его нужно вставлять в:

`БЛОК 1. CURRENT TASK`

Этот блок является основной задачей. Именно он определяет, что нужно сделать.

Сообщение 2 — короткая команда агенту.

Её нужно вставлять в:

`БЛОК 2. PREVIEW / SHORT COMMAND`

Этот блок не является самостоятельной задачей. Он только помогает агенту быстро понять:

- какой файл открыть;
- какие разделы прочитать;
- какие файлы можно менять;
- что нельзя трогать;
- куда вернуть отчёт.

Агент должен:

1. Открыть `Documentation/SCHEDULE_TASK_LOOP.md`.
2. Прочитать `БЛОК 1. CURRENT TASK`.
3. Прочитать `БЛОК 2. PREVIEW / SHORT COMMAND`.
4. Прочитать `SYSTEM INSTRUCTIONS`.
5. Прочитать `DOCUMENT STRUCTURE NOTES`.
6. Прочитать `DATE CALCULATION RULES`, если задача связана с датами или графиками.
7. Прочитать `TASK FORMAT RULES`.
8. Выполнить только задачу из `БЛОК 1. CURRENT TASK`.
9. Проверить результат по разделу `REVIEW`.
10. После выполнения обновить `AGENT REPORT`.
11. Вернуть отчёт в чат.

Важное правило:

Разовая задача должна находиться только в `БЛОК 1. CURRENT TASK`.

Короткая команда должна находиться только в `БЛОК 2. PREVIEW / SHORT COMMAND`.

Нельзя вставлять разовые задачи в начало файла выше `БЛОК 1. CURRENT TASK`.

Если в задаче указан коммит, но агент работает локально, коммит не означает обязательное переключение Git. Коммит нужен для ориентира. Работать нужно с текущими локальными файлами, если пользователь не дал отдельную команду переключиться на конкретный коммит.

После успешного выполнения задачи агент должен:

- кратко записать последнюю выполненную задачу в `PREVIOUS TASK`;
- если задача важная для истории проекта, добавить краткую запись в `TASK HISTORY`;
- не удалять старые записи из `TASK HISTORY` без отдельного указания пользователя.

======================================================================
                           PREVIOUS TASK
======================================================================

Предыдущая выполненная задача:

Выполнен анализ соответствия разделов `2.3. Календарно -тематический план` и `2.4. Общее содержание программы` в базовой программе.

- Файл анализа: `Программа/72_Базовая_Бартов_2026.docx`.
- Приоритетным источником признан раздел `2.3`.
- Выявлено частичное соответствие `2.4` разделу `2.3`.
- Основной разрыв: в `2.4` отсутствует блок по слайсерам, Orca и 3D-печати.
- Для следующего шага достаточно править только `2.4`; `2.3` можно оставить без изменений.

======================================================================
                            TASK HISTORY
======================================================================

Раздел содержит краткую историю важных выполненных задач.

## 2026-05-13 — создание и настройка SCHEDULE_TASK_LOOP.md

- Создан рабочий файл `Documentation/SCHEDULE_TASK_LOOP.md`.
- Добавлена двухблочная структура задач:
  - `БЛОК 1. CURRENT TASK`;
  - `БЛОК 2. PREVIEW / SHORT COMMAND`.
- Добавлены постоянные правила для агента.
- Добавлены правила про временные Word-файлы `~$*.docx` и `~$_*.docx`.
- Добавлены правила расчёта дат по производственному календарю РФ.

## 2026-05-13 — первый график Приложения 2

- Обновлён первый календарный график Приложения 2.
- График 1: понедельник / среда.
- Количество занятий: 32.
- Первая дата: `02.09.2026`.
- Последняя дата: `23.12.2026`.
- `04.11.2026` пропущено как официальный нерабочий праздничный день РФ.
- Второй график в этой задаче не изменялся.

## 2026-05-13 — правило про локальную работу

- Добавлено постоянное правило: если агент работает локально, не скачивать файлы из GitHub и не переключать коммит без отдельного указания пользователя.
- Добавлен раздел `LOCAL WORK RULES`.
- Уточнено, что указанный коммит при локальной работе является ориентиром, а не командой на переключение.
- Добавлена проверка, что локальные файлы не заменялись версиями из GitHub без указания пользователя.

## 2026-05-13 — оба графика Приложения 2

- Обновлены оба календарных графика Приложения 2 под учебный сезон 2026.
- График 1: понедельник / среда, `02.09.2026` — `23.12.2026`.
- График 2 сначала был ошибочно рассчитан с `04.09.2026` по `22.12.2026`.
- После правки DOCX был проверен через Microsoft Word.

## 2026-05-13 — исправление второго графика

- Исправлен только второй календарный график Приложения 2.
- График 2: вторник / пятница.
- Количество занятий: 32.
- Первая дата исправлена на `01.09.2026`.
- Последняя дата исправлена на `18.12.2026`.
- График 1 не изменялся.
- DOCX проверен повторным открытием в Microsoft Word без предупреждения о нечитаемом содержимом.

## 2026-05-14 — анализ 72_Базовая_Бартов_2026.docx

- Выполнен только анализ файла `Программа/72_Базовая_Бартов_2026.docx`.
- Найден раздел `2.3. Календарно -тематический план`.
- Таблица раздела `2.3` содержит `32` строки занятий и `32` даты.
- В таблице ровно `32` занятия.
- Столбец с датами находится во второй колонке `Дата`.
- Для следующего шага можно безопасно менять только даты под график понедельник / четверг с `03.09.2026`.

## 2026-05-14 — обновление раздела 2.3 в базовой программе

- В файле `Программа/72_Базовая_Бартов_2026.docx` обновлены даты раздела `2.3. Календарно -тематический план`.
- График: понедельник / четверг.
- Количество занятий: `32`.
- Первая дата: `03.09.2026`.
- Последняя дата: `21.12.2026`.
- Заменены только даты во второй колонке `Дата`.
- Темы занятий, часы, нумерация занятий и нецелевые таблицы не изменялись.
- DOCX проверен повторным открытием в Microsoft Word без предупреждения о нечитаемом содержимом.

## 2026-05-14 — обновление раздела 2.3 в продвинутой программе

- В файле `Программа/72_Продвинутая Бартов_2026.docx` обновлены даты раздела `2.3. Календарно -тематический план`.
- График: вторник / пятница.
- Количество занятий: `32`.
- Первая дата: `01.09.2026`.
- Последняя дата: `18.12.2026`.
- Заменены только даты в колонке `Дата`.
- Темы занятий, часы, нумерация занятий и нецелевые таблицы не изменялись.
- DOCX проверен повторным открытием в Microsoft Word без предупреждения о нечитаемом содержимом.

## 2026-05-14 — анализ соответствия разделов 2.3 и 2.4 в базовой программе

- Выполнен только анализ файла `Программа/72_Базовая_Бартов_2026.docx` без изменения DOCX.
- Сравнены разделы `2.3. Календарно -тематический план` и `2.4. Общее содержание программы`.
- Установлено, что `2.4` лишь частично соответствует `2.3`.
- Самое существенное несоответствие: в `2.4` полностью отсутствует цепочка тем по слайсерам, Orca, подготовке модели к печати и запуску 3D-печати.
- Также выявлены размытые формулировки в части командного проекта и слишком широкое описание блока Unreal Engine 5.
- Для исправления достаточно отдельной текстовой правки `2.4`; раздел `2.3` менять не требуется.

======================================================================
                              REVIEW
======================================================================

Общие проверки после выполнения задач:

1. Изменены только файлы, явно указанные в `БЛОК 1. CURRENT TASK`.
2. DOCX-файл не переписан с нуля.
3. Если задача не требовала изменения DOCX — DOCX не изменён.
4. Если задача не требовала изменения графиков — графики не изменены.
5. Если задача относится только к первому графику — второй график не изменён.
6. Количество занятий не изменено без отдельного указания.
7. Темы занятий не изменены без отдельного указания.
8. Количество часов не изменено без отдельного указания.
9. Временные файлы Word вида `~$*.docx` и `~$_*.docx` не анализировались, не редактировались и не удалялись вручную.
10. Файл `Documentation/SCHEDULE_TASK_LOOP.md` остаётся читаемым в UTF-8.
11. Разовые задачи не добавлены выше `БЛОК 1. CURRENT TASK`.
12. Большая задача находится только в `БЛОК 1. CURRENT TASK`.
13. Короткая команда находится только в `БЛОК 2. PREVIEW / SHORT COMMAND`.
14. Разделители между блоками сохранены.
15. Если агент работает локально, проверить, что локальные файлы не были заменены версиями из GitHub без отдельного указания пользователя.
16. Если задача важная для истории проекта, проверить, что запись добавлена в `TASK HISTORY`.
17. Старые записи из `TASK HISTORY` не удалены без отдельного указания пользователя.

======================================================================
                            AGENT REPORT
======================================================================

### DONE

- Выполнен аналитический разбор разделов `2.3` и `2.4` в файле `Программа/72_Базовая_Бартов_2026.docx`.
- Извлечены темы, блоки и логика раздела `2.3`, затем сопоставлены с раскрытием тем в `2.4`.
- Подготовлен список смысловых расхождений, пропусков, дублей и спорных мест.
- Очищены `БЛОК 1. CURRENT TASK` и `БЛОК 2. PREVIEW / SHORT COMMAND`.
- Обновлены `PREVIOUS TASK`, `TASK HISTORY` и `AGENT REPORT`.

### CHANGED FILES

- `Documentation/SCHEDULE_TASK_LOOP.md`

### ANALYSIS

- Общий вывод: `2.4` соответствует `2.3` только частично. Начальный блок по Blender раскрыт в целом правильно, но середина и конец раздела `2.4` описаны неполно и местами смещают логику программы.
- Соответствие `2.4` разделу `2.3`: неполное. Самый крупный пропуск — полное отсутствие тем по слайсерам, Orca, подготовке модели к печати и запуску 3D-печати.
- Насколько требуется правка: правка заметная, но локальная. Переписывать весь DOCX не нужно, достаточно содержательно переработать `2.4`.
- Можно ли оставить `2.3` без изменений: да. Раздел `2.3` выглядит последовательным и может оставаться опорной структурой для переписывания `2.4`.

### MISMATCH TABLE

| № | Тема / блок из 2.3 | Что написано в 2.4 | Проблема | Что нужно исправить |
|---|---|---|---|---|
| 1 | Блок II: занятия 17-21, обсуждение и реализация командного проекта | Темы 8-9 есть, но тема 9 снова перечисляет оптимизацию, UV, запекание и текстурирование | Проектный этап раскрыт слишком общо и частично дублирует блок I вместо описания командной логики, ролей и интеграции | Переписать темы 8-9 так, чтобы в центре были командная работа, распределение задач, сборка общего результата и контроль стиля |
| 2 | Занятие 22: `Финализация графической части проекта` | Тема 10 описана как краткий анализ работ и доработка | Слабое раскрытие ожидаемого результата; внутри темы есть несостыковка между заголовком `2 часа` и суммой теории/практики | Уточнить, что входит в финализацию, какой результат должен быть получен, и привести часы к единой логике |
| 3 | Занятия 23-25: слайсеры, интерфейс Orca, настройки печати, подготовка и запуск 3D-печати | В `2.4` соответствующих тем нет | Полностью отсутствует целый тематический блок, хотя в `2.3` он занимает три занятия и связывает Blender с последующей работой | Добавить отдельную тему или подпоследовательность тем между финализацией графики и импортом в Unreal Engine 5 |
| 4 | Переход от блока II к блоку III | После финализации `2.4` сразу переходит к импорту в Unreal Engine 5 | Нарушен порядок программы: теряется технологический шаг между завершением модели и переносом в движок | Восстановить в `2.4` промежуточный этап подготовки моделей к печати и печати как часть общей логики курса |
| 5 | Занятие 26: `Импортирование 3D моделей командного проекта в Unreal Engine 5` | Тема 11 есть, но выделена шире и на `2,5 часа` | Граница темы размыта; описание и объём выглядят сдвинутыми из-за пропущенного 3D-печатного блока | Оставить в теме 11 только импорт, материалы, коллизию и файловую структуру; объём и границы согласовать с `2.3` |
| 6 | Занятия 27-31: `Работа с блупринтами... Настройка персонажа` | Тема 12 добавляет `различные игровые механики` и `создание интерактивных объектов` | Формулировка шире и более продвинута, чем в `2.3`; может увести текст в сторону от базовой программы | Сузить описание до настройки персонажа, базовой логики взаимодействия и тех действий, которые прямо следуют из `2.3` |
| 7 | Общая структура 2.3: три блока, 32 занятия | В `2.4` один модуль `Продвинутое 3D моделирование... (76 часов)` | Структура `2.4` слабо отражает трёхблочную логику `2.3`; число `76 часов` также выглядит спорно относительно фактической детализации тем | При правке `2.4` заново выстроить подпоследовательность по блокам `2.3` и проверить согласованность заголовка модуля с содержанием |

### WEAKLY COVERED 2.3 TOPICS

- `Ознакомление и обсуждение командного проекта. Дифференцированное распределение задач` раскрыто слишком кратко и без явных критериев распределения ролей.
- `Реализация командного проекта` описана общо и частично повторяет уже пройденные моделирующие операции вместо акцента на совместной сборке результата.
- `Финализация графической части проекта` раскрыта слабо: не хватает конкретики по финальной проверке, исправлению ошибок и подготовке итогового набора материалов.
- `Знакомство со слайсерами. Изучение интерфейса Orca. Обзор основных технологий 3D печати` в `2.4` отсутствует полностью.
- `Изучение основных настроек для печати 3D моделей... Подготовка и выгрузка 3D модели` в `2.4` отсутствует полностью.
- `Запуск печати 3D модели` в `2.4` отсутствует полностью.
- `Импортирование 3D моделей... в Unreal Engine 5` есть, но требует более чёткого отделения от пропущенного 3D-печатного блока.

### EXTRA OR QUESTIONABLE 2.4 TOPICS

- `Разработка различных игровых механик взаимодействия с объектами` выглядит шире, чем формулировки `2.3`, и тянет текст в более продвинутую сторону.
- `Создание интерактивных объектов` не выделено как отдельная тема в `2.3` и потому выглядит спорным расширением.
- В теме 9 снова перечислены оптимизация, UV, запекание и текстурирование, хотя эти операции уже отдельно раскрыты в темах 3-7; это создаёт смысловой дубль.
- Заголовок модуля `Продвинутое 3D моделирование в игровой индустрии` и пометка `(76 часов)` выглядят спорно относительно логики базовой программы и текущей детализации тем внутри `2.4`.

### RECOMMENDATION

- Следующей задачей нужно править именно раздел `2.4. Общее содержание программы`.
- Раздел `2.3. Календарно -тематический план` можно оставить без изменений.
- В `2.4` нужно переписать блок тем 8-12: отдельно усилить описание командного проекта, восстановить пропущенный 3D-печатный этап и сузить формулировки по Unreal Engine 5 до уровня `2.3`.
- Нужна отдельная задача на текстовую правку `2.4`, а не на изменение дат, часов или структуры `2.3`.

### DIFF / SUMMARY

- DOCX не изменялся.
- Раздел `2.3` не изменялся.
- Раздел `2.4` не изменялся.
- Даты не менялись.
- Темы, часы и номера занятий не менялись.
- `БЛОК 1. CURRENT TASK` и `БЛОК 2. PREVIEW / SHORT COMMAND` очищены.
- `Documentation/SCHEDULE_TASK_LOOP.md` обновлён.
- `PREVIOUS TASK` обновлён.
- `TASK HISTORY` обновлён.
- GitHub не использовался для замены локальных файлов.
- Git-коммит не переключался.
- Временные файлы Word не трогались и не попали в изменения.

### PROBLEMS

- Нет.

### QUESTIONS

- Нет.
